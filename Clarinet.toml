[project]
name = "init-authenticator"
authors = ["Developer Name"]
description = "A blockchain-native authentication and asset verification platform"
telemetry = false
requirements = []
[contracts.init-authenticator]
path = "contracts/init-authenticator.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
